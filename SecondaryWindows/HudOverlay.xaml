<Window x:Class="SecondaryWindows.HudOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="HudOverlay"
        SizeToContent="WidthAndHeight"
        MinWidth="225"
        MinHeight="80"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="Manual"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <Border x:Name="HudBorder"
            Background="#D0161616"
            BorderBrush="#5544AACC"
            BorderThickness="1"
            CornerRadius="10"
            Padding="10"
            Margin="10"
            MouseLeftButtonDown="HudBorder_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <Border.LayoutTransform>
            <ScaleTransform x:Name="HudScale" ScaleX="1" ScaleY="1" />
        </Border.LayoutTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="MODE"
                       Foreground="#88FFFFFF"
                       FontFamily="Atkinson Hyperlegible"
                       FontSize="10"
                       Grid.Row="0"
                       Grid.Column="0" />
            <TextBlock x:Name="ModeTextBlock"
                       Text="Aim: Hold"
                       Foreground="White"
                       FontFamily="Atkinson Hyperlegible"
                       FontSize="11"
                       Grid.Row="0"
                       Grid.Column="1"
                       TextTrimming="CharacterEllipsis" />

            <TextBlock Text="PATTERN"
                       Foreground="#88FFFFFF"
                       FontFamily="Atkinson Hyperlegible"
                       FontSize="10"
                       Grid.Row="1"
                       Grid.Column="0" />
            <TextBlock x:Name="PatternTextBlock"
                       Text="none"
                       Foreground="White"
                       FontFamily="Atkinson Hyperlegible"
                       FontSize="11"
                       Grid.Row="1"
                       Grid.Column="1"
                       TextTrimming="CharacterEllipsis" />

            <StackPanel Grid.Row="2"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        Margin="0,8,0,0">
                <Ellipse x:Name="AimDot"
                         Width="8"
                         Height="8"
                         Fill="IndianRed"
                         VerticalAlignment="Center"
                         Margin="0,0,6,0" />
                <TextBlock x:Name="AimStatusTextBlock"
                           Text="Aim locked"
                           Foreground="White"
                           FontFamily="Atkinson Hyperlegible"
                           FontSize="11"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
